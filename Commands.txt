Run server:
    php artisan serve

Make roles and default admin account:
    php artisan db:seed --class=CreateRolesSeeder 

        'email' => admin@admin
        'password' => adminPassword
        